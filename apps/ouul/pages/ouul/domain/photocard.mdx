import { PhotoCardAttributes } from '../../../src/constant/ouul';
import { HStack, Parameters } from '@docs/ui';

### PhotoCard란?

`포토카드` 는 1개의 펫에 대해 여러개의 포토카드 생성이 가능합니다.<br />
인쇄신청을 하면 `실물포토`로도 생성됩니다.


### PhotoCardModel

<Parameters title="데이터 형식" attributes={PhotoCardAttributes} />
